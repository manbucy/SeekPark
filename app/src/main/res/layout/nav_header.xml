<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="?attr/colorPrimary"
    android:padding="10dp">

    <TextView
        android:id="@+id/nav_header_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/nav_header_mail"
        android:text="Tony"
        android:textColor="#fff"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/nav_header_mail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="thisistony@gmail.com"
        android:textColor="#fff"
        android:textSize="14sp" />

    <ImageView
        android:id="@+id/nav_header_head_image"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_above="@+id/nav_header_username"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="24dp" />

</RelativeLayout>